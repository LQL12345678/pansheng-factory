# 2026-05-13 工作记录

## 自动化任务

### 每日自动启动设备管理系统（09:05）
- 后端服务（:3001）和前端服务（:5173）均未运行，已重新启动
- 后端 `node server.js` 启动成功，数据库初始化完成
- 前端 `npm run dev`（Vite v6.4.2）启动成功，1446ms 就绪
- 已通过 preview_url 展示 http://localhost:5173 界面

## 首页快捷按钮调整（09:17）
- 修改 `src/pages/HomePage.tsx`：quickActions 数组顺序改为 新增工单→零部件领用→新增零部件→新增设备
- 修改 `src/index.css`：移动端 `.quick-actions` 由横向滚动改为 `grid` 两列布局，使4个按钮呈2×2排列

## 零部件库存数据导入（09:58）
- 从 `C:/Users/Admin/Desktop/零部件库存.xlsx`（BD设备零部件台账）导入102条数据
- 给 `parts` 表新增 `category` 字段（物品类别）
- 执行 `backend/import_parts.js` 脚本，按类别字母排序（23个类别）导入，part_no 格式 PART-0001~PART-0102
- 更新 `backend/routes/parts.js` GET接口：按 category/part_no/id 排序；POST/PUT接口支持 category 字段
- 更新 `frontend/src/types/index.ts` Part接口：新增 category 字段
- 后端重启生效，验证API返回102条数据且按类别排序正确

## 新增行内库存编辑 + 彻底修复删除（10:20）
- 发现旧后端进程（PID 13320）用 taskkill /F /IM 无法杀掉，需要用 taskkill //F //PID <PID> 指定进程
- 发现 PUT /:id 接口漏写了 stock 字段更新，编辑弹窗改了库存也不会保存
- 修复：
  1. backend/routes/parts.js：PUT /:id 加入 stock 字段；新增 PATCH /:id/stock 专用快速更新库存接口；DELETE /:id 加 try/catch
  2. frontend/src/services/api.ts：partApi 新增 patchStock(id, stock) 方法
  3. frontend/src/pages/PartList.tsx：库存列点击数字变行内输入框，回车/失焦保存（调用 PATCH 接口），Escape 取消
- 库存列表头加 ✏️ 提示，可点击的库存数字有下划线+✏ 标识
- 根因：`node:sqlite` 不接受 `undefined` 参数，`applicable_devices`、`brand`、`unit_price`、`supplier`、`purchase_cycle_days` 等可选字段未传时为 `undefined` 导致 `TypeError: Provided value cannot be bound to SQLite parameter 5`
- 修复：`backend/routes/parts.js` 的 POST 和 PUT 接口中，所有可选字段改用 `?? null` 或 `?? ''` 处理，并加 try/catch 返回 500 错误信息
- 后端重启验证通过：含可选字段和省略可选字段的 PUT 请求均返回 `{"success":true}`

## 备件领用/入库/记录/搜索功能完整实现（10:57）

### 后端（backend/routes/parts.js）
- `POST /parts/usage`：领用接口，校验库存不足，扣减库存+写 inventory_logs（新增 line/purpose 字段）
- `POST /parts/stockin`：入库接口，增加库存+写 inventory_logs
- `GET /parts/usage`：领用记录查询，支持 keyword/start_date/end_date 筛选
- `GET /parts/stockin`：入库记录查询，支持同样筛选
- `GET /parts?keyword=`：搜索接口，模糊匹配 part_no/name/spec/brand/storage_location/applicable_devices
- db.js：ALTER TABLE inventory_logs 新增 line/purpose 两列（用 try/catch 忽略已存在错误）

### 前端（frontend/src/pages/PartList.tsx 完整重写）
- 操作列：编辑 | 删除 | 领用（蓝色）| 入库（绿色）
- 领用弹窗：领用人、领用数量、线体（下拉预设8条+其他）、领用日期、用途、备注；库存不足前端+后端双重校验
- 入库弹窗：入库数量、入库日期、录入人、备注
- 📋 领用/入库记录按钮：弹出大弹窗，Tab切换领用/入库两个表格，支持关键字+日期范围筛选+查询按钮
- 顶部搜索框：输入框+搜索+重置，回车触发，调用后端关键字过滤接口
- 所有提交按钮加 loading 状态（提交中...），防止重复点击
- 所有弹窗点击遮罩不关闭

### frontend/src/services/api.ts
- partApi 新增：list(keyword?)、usage()、stockin()、usageLogs()、stockinLogs()

## 弹窗点击遮罩不关闭（10:37）
- 涉及文件：DeviceList.tsx / RepairList.tsx / MaintenancePlan.tsx / PartList.tsx（共2处）
- 修改：去掉所有 `modal-overlay` 的 `onClick` 关闭回调，同步去掉内部 `div.modal` 的 `onClick={e => e.stopPropagation()}`
- 效果：弹窗只能通过底部「保存」或「取消」按钮关闭，点击遮罩区域无效

## 修复删除失败 + 去掉虚线下划线 + 序号列降序（10:30）
- 根因：`inventory_logs` 表有外键 `FOREIGN KEY (part_id) REFERENCES parts(id)`，且 `PRAGMA foreign_keys = ON`，直接 DELETE parts 记录会报外键约束错误
- 修复：`backend/routes/parts.js` DELETE /:id 先删 `inventory_logs WHERE part_id=?`，再删 `parts WHERE id=?`
- 前端 `PartList.tsx`：库存数字的 `borderBottom: '1px dashed #aaa'` 虚线下划线改为无样式
- 前端 `PartList.tsx`：`load()` 改为按 id 降序排列（最新新增在最前），表格新增序号列（1,2,3...），宽48px居中显示
- 后端所有旧 node 进程强杀后重启，前端 vite dev 同步重启

## 修复库存页面编辑/删除无效（10:20）
- 根因：WorkBuddy 内置浏览器面板在 iframe 中运行，`window.alert()` 和 `window.confirm()` 被浏览器安全策略阻止，导致：
  - 编辑保存后调用 `alert('更新成功')` 被阻断，页面表现为静默（实际数据已保存）
  - 删除按钮调用 `window.confirm('确认删除?')` 被阻断，返回 false，导致删除逻辑从未执行
- 修复：
  1. 将 `window.alert` 替换为内联 Toast 提示（右上角固定定位，3秒自动消失）
  2. 将 `window.confirm` 删除确认替换为自定义 Modal 弹窗（`confirmDelete` 状态控制）
  3. `handleSubmit` 中用 `const isEdit = !!editItem?.id` 提前保存编辑状态，避免 setState 后闭包丢失
  4. `purchase_cycle_days` 在提交时补充 `Number()` 转换
  5. CSS 新增 `@keyframes fadeIn` 动画
- 文件：`frontend/src/pages/PartList.tsx`、`frontend/src/index.css`

## 备件列表按钮样式与布局优化（11:12）

### 操作列按钮顺序调整
- 原顺序：编辑 → 删除 → 领用 → 入库
- 新顺序：领用 → 入库 → 编辑 → 删除（高频操作前置）

### 工具栏按钮重排
- 原布局：[新增备件] [导入Excel] [导出Excel] [领用/入库记录]
- 新布局：[新增备件] [领用/入库记录] [导入Excel] [导出Excel]

### 新增 CSS 按钮样式类（深色 + 浅色主题）
- `btn-outline-primary`：主色线框按钮（青色边框，用于「领用/入库记录」）
- `btn-outline-default`：灰色线框按钮（用于「导入/导出Excel」）
- `btn-text`：文字按钮（灰色无背景，用于「重置」）
- `btn-action`：表格内操作按钮基础类（无背景无边框文字按钮）
  - `btn-usage`：领用（绿色 #10B981），hover 绿色半透明背景
  - `btn-stockin`：入库（橙色 #F59E0B），hover 橙色半透明背景
  - `btn-edit`：编辑（灰色），hover 亮灰背景
  - `btn-delete`：删除（红色 #EF4444），hover 红色半透明背景
- `.toolbar-search`：搜索区域容器，搜索框聚焦时宽度自适应展开
- 所有按钮统一标准：高度 28-32px，圆角 6-8px，内边距 左右 12-18px 上下 4-8px，字体 13-14px
- 表格内按钮间距 10px，hover 有颜色反馈效果

### 修改文件
- `frontend/src/pages/PartList.tsx`：操作列和工具栏
- `frontend/src/dark-theme.css`：深色主题按钮样式 + 移动端适配
- `frontend/src/index.css`：浅色主题按钮样式 + 移动端适配

## 维修工单系统重构（11:30）

### 后端（backend/routes/repairs.js）
- 新增 `POST /:id/start`：标记处理中，记录 accept_time
- 新增 `POST /:id/complete`：完成工单，记录处理结果、更换备件、维修人、用时、停线时长；自动扣减备件库存并写入领用记录（inventory_logs）
- 新增统计接口：
  - `GET /stats/overview`：今日新增、进行中、本周完成率、平均修复时间（MTTR）
  - `GET /stats/high-frequency`：高频故障设备排行（最近90天）
  - `GET /stats/monthly-trend`：月度趋势（最近6个月）
- 修改 `GET /` 接口：支持 status 和 search 参数筛选

### 前端（frontend/src/pages/RepairList.tsx 重写）
- 统计看板：4个卡片（今日新增、进行中、本周完成率、平均修复时间）+ 高频故障设备排行表 + 月度趋势简易柱状图
- 工具栏：搜索框（防抖500ms）+ 状态筛选下拉
- 表格操作列：根据状态显示「标记处理中」或「完成」按钮
- 完成弹窗：填写处理结果、维修人、用时、停线时长；搜索并添加更换备件（自动扣减库存）
- 新增报修弹窗：简化表单（线体、设备、故障描述、报修人）
- 编辑弹窗：完整字段表单
- API 更新：`repairApi` 新增 start()、complete()、statsOverview()、statsHighFreq()、statsMonthlyTrend()

### 其他
- `frontend/src/services/api.ts`：更新 repairApi 方法
- 构建验证：前端 vite build 成功


## 修复新增工单/报销无法保存（12:30）

### 根因
- `repairs` 表 `repairman` 列有 `NOT NULL` 约束（db.js 第50行 `repairman TEXT NOT NULL`）
- 重写 RepairList.tsx 时，新增报修表单只保留了4个字段（线体/设备/故障描述/报修人），没有 `repairman`
- 提交时 `repairman` 为 undefined → 后端 `d.repairman || null` 变成 null → SQLite NOT NULL 约束报错
- 前端 `alert()` 在 iframe 中被浏览器安全策略屏蔽 → 用户看不到错误信息，表现为"无法保存"

### 修复
1. `backend/db.js`：init() 末尾新增迁移脚本，检测 `repairman` 列有 NOT NULL 时重建表去掉约束；同时修改 CREATE TABLE 定义去掉 NOT NULL
2. `backend/routes/repairs.js`：POST / PUT / DELETE 接口全部加 try/catch，返回 500 + error message（不再让 Express 默认返回 HTML 错误页）
3. 修复 JS 文件中误写 TypeScript 语法 `catch (e: any)` / `(c: any)` 导致后端启动失败的错误
4. 重启后端验证：POST 不带 repairman 字段成功返回 `{"id":230,"work_order_no":"WO-2026-05-006"}`


## 统计看板模块调整（12:40）
- RepairList.tsx：去掉高频故障设备排行和月度趋势图表，只保留4个统计卡片（今日新增/进行中/本周完成率/平均修复时间）
- StatsDashboard.tsx：新增"设备月度趋势"模块（最近6个月柱状图，显示总数vs完成数），调用 repairApi.statsMonthlyTrend() 接口
- 移除 RepairList.tsx 中的 useMemo import（不再需要 trendMax 计算）

## 修复维修工单删除和搜索失效（12:45）

### 根因
1. **删除失效**：RepairList.tsx 第338行使用 `window.confirm()` 做删除确认，WorkBuddy iframe 环境中 `window.confirm()` 被安全策略拦截，返回后无反应
2. **搜索失效**：搜索防抖逻辑有 `if (search !== '')` 条件，清空搜索框时不会触发 load()，导致列表无法恢复

### 修复
1. 将 `window.confirm()` 替换为自定义确认弹窗（`deleteConfirm` 状态控制），与新增/完成弹窗风格一致
2. 去掉搜索防抖中的 `if (search !== '')` 条件，清空搜索框也能正常重新加载
3. 前端 vite build 验证通过

## 维修工单新增功能（13:00）

### 工单分类（work_order_type）
- 数据库新增 `work_order_type` 字段（设备维修/工具维修，默认设备维修）
- db.js：repairs_new 和 repairs_no_nn 迁移表均加入 work_order_type 列
- db.js：repairs_new 迁移包裹 try/catch，列数不匹配时跳过并清理
- db.js：新建 repair_parts 表（id, repair_id, part_id, part_name, quantity, created_at），外键级联删除
- 后端 GET /repairs 支持 work_order_type 筛选（COALESCE(NULL, '设备维修')）
- 后端 POST/PUT /repairs 支持 work_order_type 字段

### 完成工单事务处理
- POST /:id/complete 重写为事务：BEGIN → 更新工单 → 保存repair_parts → 扣库存 → 写inventory_logs → COMMIT
- 库存不足返回 400 错误并 ROLLBACK，不允许完成
- 零件不存在同样 ROLLBACK
- 删除旧的 repair_parts 记录（支持重新完成）

### 统计看板分类统计
- GET /stats/overview 支持 work_order_type 筛选（COALESCE 处理 NULL）
- GET /stats/monthly-trend 按 month + work_order_type 分组，返回对比数据
- StatsDashboard.tsx：新增类型筛选器（全部/设备维修/工具维修）
- StatsDashboard.tsx：统计卡片（今日新增/进行中/完成率/MTTR）跟随类型筛选
- StatsDashboard.tsx：「全部」模式下月度趋势显示设备维修vs工具维修对比（4色柱状图），单类型模式显示总数vs完成

### 前端 RepairList.tsx
- 工具栏新增「全部类型/设备维修/工具维修」下拉筛选
- 新增报修表单和编辑表单均添加「工单类型」下拉选择
- formData 初始化默认 work_order_type: '设备维修'

### api.ts 更新
- repairApi.statsOverview 和 statsMonthlyTrend 改为接受 params 参数

### genWO 修复
- 工单号格式 WO-YYYY-MM-NNN 有4段，原 split('-')[2] 取的是月份而非序号
- 改为 split('-')[parts.length-1] 取最后一段（序号）

## 工单详情弹窗 + 备件搜索优化（13:30）

### 问题排查
- 用户反馈：完成工单时使用零部件后库存没有自动扣减
- 排查结果：后端事务逻辑完全正确（BEGIN/COMMIT/ROLLBACK + repair_parts INSERT + 库存扣减 + inventory_logs 写入均正常）
- 通过 API 直接测试 POST /:id/complete，库存从10→9，repair_parts 记录正确写入
- 之前测试数据因 WAL 模式下强杀进程导致部分数据丢失，非代码问题

### 新增：查看详情弹窗
- RepairList.tsx 操作列新增「详情」按钮（所有状态可见）
- 详情弹窗展示完整工单信息：工单号、类型、线体、设备、报修/维修人、日期时间、故障信息等
- 底部展示「更换零部件」区域：表格显示备件名称、编号(part_no)、数量；无记录时显示提示文案
- 显示总零件件数统计

### 优化：完成工单备件搜索
- 备件搜索改为自动搜索（防抖300ms），去掉手动「搜索」按钮
- 搜索下拉改为浮动定位（position: absolute），显示规格、库存、存放位置
- 选中备件后显示带边框的确认区域，含数量输入（min=1, max=库存）和「添加」「取消」按钮

### 后端修改
- GET /repairs 的 repair_parts 查询改为 LEFT JOIN parts 获取 part_no 字段

### 类型定义更新
- types/index.ts：新增 RepairPart 接口（含 part_no）；Repair 接口新增 parts、work_order_type 字段

## 删除操作统一加入密码确认（13:38）

### 新增组件
- `frontend/src/components/DeleteConfirmWithPassword.tsx`：通用密码确认删除弹窗
  - 删除前要求输入管理密码（硬编码 8520）才能确认
  - 支持 title/message/itemName/onConfirm/onCancel/loading props
  - 密码错误时红色边框 + 错误提示，自动清空并聚焦
  - 支持 Enter 确认、Escape 取消
  - 密码输入框居中显示，letter-spacing: 8 增强隐私感

### 替换的5个页面
1. `DeviceList.tsx`：window.confirm → DeleteConfirmWithPassword，显示设备名称
2. `RepairList.tsx`：自定义 Modal → DeleteConfirmWithPassword，删除 handleDelete 函数（逻辑内联到组件）
3. `PartList.tsx`：自定义 Modal → DeleteConfirmWithPassword，显示备件名称
4. `MaintenancePlan.tsx`：window.confirm → DeleteConfirmWithPassword
5. `gen_repair.js`：window.confirm → DeleteConfirmWithPassword（代码生成模板同步更新）

### 验证
- TypeScript 编译零错误
- vite build 构建成功（1.64s）

## 修复筛选功能 + 数据库迁移问题（13:50）

### 问题1：前端 useEffect 依赖不完整
- `RepairList.tsx` 第68行 `useEffect(() => { load(); }, [statusFilter])` 只监听 statusFilter
- 修复：依赖改为 `[statusFilter, typeFilter, search]`
- 同时删除多余的搜索防抖 useEffect（避免重复请求）

### 问题2：数据库 work_order_type 列数据错乱（根因）
- 后端数据库 `backend/db/equipment.db`（不是根目录的 devices.db）
- db.js 迁移脚本用 `INSERT INTO repairs_new SELECT * FROM repairs` 做表结构升级
- 旧表13列 vs 新表21列，`SELECT *` 按位置映射导致列错位
- 结果：`work_order_type` 列存的是 `created_at` 的时间戳，`created_at` 列存的是 DEFAULT 值"设备维修"
- 修复：UPDATE 56条记录，将 work_order_type 的时间戳改为"设备维修"，恢复 created_at

### 问题3：迁移脚本会重复破坏数据
- db.js 中的 `repairs_new` 迁移每次启动都会执行 `SELECT *` 拷贝
- 修复：迁移前先检查 `work_order_type` 列是否存在，存在则跳过
- `INSERT INTO repairs_new SELECT * FROM repairs` 改为显式列映射（用 COALESCE 处理旧字段名 problem_desc→fault_desc, handler→repairman）

## 设备台账：报废功能 + 采购/报废记录（14:20）

### 数据库（backend/db.js）
- 新建 `device_change_records` 表：id, device_id, device_no, device_name, change_type(采购/报废), change_date, operator, reason, remarks, created_at
- init() 末尾新增迁移逻辑：为现有所有设备自动回填采购记录（change_date 用 install_date 或 created_at）

### 后端路由（backend/routes/devices.js）
- 新增 `PUT /:id/scrap`：报废设备接口，必填报废日期/原因/报废人，防重复报废，同时写 device_change_records
- 新增 `GET /change-records`：变更记录查询，支持 type(采购/报废) 和 keyword 筛选
- 修改 `POST /`：新增设备后自动写一条采购记录
- 修改 `POST /import`：Excel导入成功后自动写采购记录
- **关键**：`GET /:id` 路由加 `([0-9]+)` 正则约束，防止 `/change-records` 被当作 `:id` 匹配

### 前端类型（frontend/src/types/index.ts）
- 新增 `DeviceChangeRecord` 接口

### 前端API（frontend/src/services/api.ts）
- deviceApi 新增 `scrap(id, data)` 和 `changeRecords(params)` 方法

### 前端页面（frontend/src/pages/DeviceList.tsx 重写）
- 操作列：编辑 | 删除 | 报废（红色）
- 报废弹窗：报废日期(date)、报废原因(textarea)、报废人(text)，确认后调 API + 刷新列表
- 采购/报废记录按钮：工具栏位于「新增设备」和「导入Excel」之间
- 记录弹窗：类型筛选(全部/采购/报废) + 关键字搜索 + 变更记录表格
- 状态扩展：正常(绿)、维修中(橙)、闲置(灰)、停用(橙)、报废(红)
- 报废行：opacity 0.5 置灰，操作按钮替换为「已报废」文字
- 新增 `status-danger` 和 `status-idle` CSS 类（App.css + dark-theme.css）

### 验证
- 变更记录API回填成功（所有现有设备自动生成采购记录）
- 类型筛选和关键字搜索API正常
- 报废API正常（状态变更+记录生成+防重复）
- 设备详情API `GET /:id([0-9]+)` 正常
- TypeScript 编译零错误，vite build 成功

## 工具台账功能实现（14:42）

### 数据库（backend/db.js）
- 新建 `tool_categories` 表：id, name, created_at
- 新建 `tools` 表：id, tool_no, name, category, model, location, production_line, supplier, purchase_date, responsible_person, status, remarks, created_at, updated_at
- 新建 `tool_change_records` 表：id, tool_id, tool_no, tool_name, change_type, change_date, operator, reason, remarks, created_at
- 插入默认工具类别：手动工具、电动工具、气动工具、测量工具、安全工具、其他

### 后端路由（backend/routes/tools.js）
- 完整CRUD：GET /, POST /, PUT /:id, DELETE /:id（先删关联change_records再删tools，解决外键约束）
- PUT /:id/scrap：报废工具，必填日期/原因/报废人，写tool_change_records
- GET /change-records：变更记录查询，支持type和keyword筛选
- GET /categories/list：工具类别列表
- GET /:id([0-9]+)：单条详情，正则约束防路由冲突
- POST /import：Excel批量导入，自动生成采购记录

### server.js
- 挂载 `/api/tools` 路由（位于保养计划之前）

### 前端类型（frontend/src/types/index.ts）
- 新增 `Tool` 和 `ToolChangeRecord` 接口

### 前端API（frontend/src/services/api.ts）
- 新增 `toolApi`：list, get, create, update, delete, scrap, changeRecords, importExcel, categories

### 前端页面（frontend/src/pages/ToolList.tsx）
- 复制DeviceList结构，字段适配：tool_no(工具编号)、name(名称)、category(类别)、model(型号)、location(存放位置)、production_line(产线)、supplier(供应商)、purchase_date(采购日期)、responsible_person(责任人)、status(状态)、remarks(备注)
- 功能完整：新增/编辑/删除(密码确认)/报废/采购报废记录/Excel导入导出
- 状态样式：正常(绿)、报废(红)、闲置(灰)、其他(橙)

### App.tsx
- 导航新增「工具台账🛠️」，位于故障维修和保养计划之间
- 路由新增 `/tools/*` → `<ToolList />`

### 验证
- TypeScript编译零错误
- 后端API全部正常：CRUD、报废、变更记录、类别列表、Excel导入
- 修复：删除时先清理tool_change_records外键关联记录

## 工具台账字段简化（15:03）

### 需求变更
- 去掉：工具编号(tool_no)、类别(category)、位置(location)、产线(production_line)、供应商(supplier)、采购日期(purchase_date)、责任人(responsible_person)、备注(remarks)
- 保留6个核心字段：线体(line)、工具名称(name)、型号(model)、设备编码(device_no)、使用工位(station)、操作（编辑/删除/报废）
- 去掉工具类别(tool_categories)表
- 新增搜索功能：按线体/名称/型号/设备编码/使用工位搜索
- 其他功能不变：采购/报废记录、Excel导入导出、报废弹窗

### 修改文件
- `backend/db.js`：tools表重建为5个业务字段+id+status+时间戳；tool_change_records去掉tool_no列；迁移逻辑检测旧表tool_no列后DROP重建
- `backend/routes/tools.js`：CRUD适配新字段，去掉categories路由，导入模板简化
- `frontend/src/types/index.ts`：Tool接口简化，ToolChangeRecord去掉tool_no
- `frontend/src/services/api.ts`：toolApi去掉categories方法
- `frontend/src/pages/ToolList.tsx`：表格6列，表单5字段，前端搜索过滤

## 工具台账新增「数量」字段（15:10）

### 需求
- 工具台账表格增加「数量」列，数量可以直接编辑
- 默认值为1

### 修改文件
- `backend/db.js`：tools表新增 `quantity INTEGER DEFAULT 1`；新增迁移逻辑检测旧表是否有quantity列，无则ALTER TABLE添加
- `backend/routes/tools.js`：POST/PUT 接口支持 quantity 字段（parseInt）；Excel导入模板增加第6列(数量)
- `frontend/src/types/index.ts`：Tool接口新增 `quantity?: number`
- `frontend/src/pages/ToolList.tsx`：
  - 表头：使用工位后加「数量」列
  - 表格行：显示 `d.quantity ?? 1`
  - 新增/编辑弹窗：增加数量输入框（type="number", min="1"）
  - 导出Excel：增加数量列
  - 搜索：覆盖数量字段匹配
  - handleInputChange：type=number时用Number()转换

### 验证
- TypeScript编译零错误
- 后端API：创建时quantity正确存入；查询返回quantity字段；已有数据自动回填默认值1
- 前端表单number类型处理正确（避免TS2367类型错误）

## 导航顺序调整（15:43）
- 修改 `frontend/src/App.tsx` 的 `navs` 数组顺序
- 最终顺序：首页→设备台账→工具台账→零部件库存→故障维修→保养计划→统计看板

## 统计看板优化（15:52）

### 后端新增接口（backend/routes/repairs.js）
- `GET /stats/monthly-trend`：增加 year 和 line 查询参数；默认最近12个月；strftime引号修复（双引号→单引号）
- `GET /stats/by-line`：新增线体维度统计API（按线体+工单类型分组，返回total/completed/completionRate）
- `GET /stats/summary`：新增汇总统计API（按工单类型分组，返回total/completed/completionRate）

### 前端API（frontend/src/services/api.ts）
- repairApi 新增 statsByLine()、statsSummary() 方法

### 统计看板全面重构（frontend/src/pages/StatsDashboard.tsx）
1. **统计卡片**：4张卡片分别显示设备维修总工单+完成数、设备维修完成率、工具维修总工单+完成数、工具维修完成率
2. **月度趋势图**：柱状图对比设备维修vs工具维修，柱顶显示工单数（整数）
3. **筛选功能**：年份下拉（全部/2024/2025/2026）+ 线体下拉（全部/1线~设备组）
4. **线体统计表格**：各线体的设备/工具工单数+完成率，完成率低于60%标红加粗
5. **合计行**：表格底部汇总各列数据，完成率低于60%标红
6. 保留原有：设备总览卡片、故障频率TOP20、故障类型分布、低库存预警

## 线体数据修复 + 保养计划数据导入（16:30）

### 线体数据统一
- repairs 表线体字段合并：1+1.0→1线, 2+2.0→2线, VIP→VIP线, 紫外打印→紫外

### 保养计划Excel导入
- 从桌面 `2026年设备保养计划表.xlsx` 导入39条记录到 maintenance_plans 表
- Excel日期序列号转换（46032→2026-01-10），无实际保养时间时status为待执行
- plan_no格式：MP-2026-001~MP-2026-039
- maintenance_type格式：楼层+线体（如"3楼线体 - 1线"）
- 33条已完成 + 6条待执行

### 问题排查：保养计划页面无数据
- 根因：上次导入数据后后端进程未重启，SQLite WAL 模式下旧进程看不到新写入的数据
- 修复：重启后端服务（强杀所有node进程后重新启动），API `/api/maintenance` 返回39条数据

## 保养计划模块优化（16:55）

### 需求
1. 去掉数据表中的设备ID列
2. "保养类型"改为"保养位置"
3. 编辑弹窗增加"更换零部件"功能（和故障维修模块一致）

### 后端修改
- `backend/db.js`：新建 `maintenance_parts` 表（id, maintenance_plan_id, part_id, part_name, quantity, created_at）
- `backend/routes/maintenance.js` 重写：
  - GET /：GROUP_CONCAT 聚合 maintenance_parts，解析为 parts 数组返回
  - POST /：保存计划 + 事务处理零部件（库存检查、扣减、写领用记录）
  - PUT /：更新计划 + 事务处理零部件（回滚旧库存、插入新记录、扣减新库存）
  - DELETE /：事务处理（恢复库存 + 删除零部件记录 + 删除计划）
  - 去掉 LEFT JOIN devices 查询

### 前端修改
- `frontend/src/types/index.ts`：MaintenancePlan 接口去掉 device_id/device_no/device_name，新增 parts 字段
- `frontend/src/pages/MaintenancePlan.tsx` 重写：
  - 表格去掉"设备"列，"保养类型"列头改为"保养位置"
  - 编辑弹窗去掉设备ID输入框
  - 编辑弹窗新增更换零部件功能：备件搜索（防抖300ms自动搜索）→ 选中 → 输入数量 → 添加/移除
  - 保存时附带 parts 数组提交后端
  - 导出Excel新增"更换零部件"列，去掉设备ID/设备名称列
- TypeScript编译零错误

## 保养位置格式统一 + 看板保养计划模块（17:05）

### 保养位置格式统一
- 将所有保养计划的 maintenance_type 楼层统一改为3楼
- 转换规则：`N楼线体 - X线` → `X线-3楼线体`，`N楼线体+设备 - X线` → `X线-3楼线体+设备`
- 涉及格式：3楼线体、4~15楼线体，设备类型包含：+提升机、+AGV
- 数据库直接更新39条记录，去重后8种标准格式：1~4线-3楼线体、1~2线-3楼线体+AGV、1~2线-3楼线体+提升机
- MaintenancePlan.tsx 编辑弹窗 placeholder 更新为 `如：1线-3楼线体、1线-3楼线体+提升机`

### 看板新增保养计划展示
- StatsDashboard.tsx 底部新增「保养计划看板」模块
- 只展示「待执行」和「执行中」状态的保养计划
- 表格列：计划编号、保养位置、保养内容(task_desc)、计划日期、负责人、状态
- 标题旁显示待执行/执行中数量统计
- 状态标签：待执行(青色)、执行中(橙色)，圆角药丸样式
- 调用 maintenanceApi.list() 获取数据
- TypeScript编译零错误

## 首页保养计划版块增加保养内容（17:13）
- HomePage.tsx 保养计划列表项：原来显示 device_name（已废弃）和 maintenance_type，改为显示 maintenance_type（保养位置）+ task_desc（保养内容）+ status（状态标签）
- 数据映射去掉 device_name，新增 taskDesc 和 status 字段
- 右侧显示状态标签（待执行/执行中），替代原来的人员和日期