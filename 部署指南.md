# 攀升工厂设备管理系统 - 部署指南

## 一、环境要求

| 组件 | 要求 |
|------|------|
| Node.js | v18 及以上（推荐 v20/v22/v24） |
| 操作系统 | Windows Server / Windows 10+ / Linux（均支持） |
| 内存 | 最低 512MB，推荐 2GB+ |
| 磁盘 | 至少 500MB 可用空间 |

> 本系统使用 Node.js 内置的 `node:sqlite` 模块，**无需**额外安装 SQLite 数据库。

## 二、部署步骤

### 1. 安装 Node.js

下载并安装 Node.js v22 LTS：
- 官网：https://nodejs.org/
- 国内镜像：https://npmmirror.com/

安装完成后验证：
```bash
node --version   # 应显示 v18 及以上
npm --version    # 应显示 9 及以上
```

### 2. 解压项目

将 `攀升设备管理系统_v2_完整包.zip` 解压到目标目录，例如：
- Windows: `D:\equipment-management`
- Linux: `/opt/equipment-management`

### 3. 安装依赖

**安装后端依赖：**
```bash
cd backend
npm install
```

**安装前端依赖：**
```bash
cd ../frontend
npm install
```

### 4. 构建前端

```bash
cd frontend
npm run build
```

构建完成后，`frontend/dist/` 目录生成静态文件。Vite 已配置代理将 `/api` 转发到后端 3001 端口。

### 5. 启动服务

**方式一：开发模式运行（Windows，双击 bat）**
```bash
# 一键启动（后端 + 前端开发服务器）
.\启动服务.bat
```

**方式二：生产模式运行（推荐）**
```bash
# 后端服务（后台运行）
cd backend
nohup node server.js > server.log 2>&1 &

# 前端构建后，可用 Nginx/Apache/IIS 代理到 dist 目录
# 或者用 serve 包：
cd frontend
npx serve dist -l 5173
```

**方式三：PM2 进程管理（推荐生产环境）**
```bash
npm install -g pm2
cd 项目根目录
pm2 start ecosystem.config.js
pm2 save
pm2 startup   # 设置开机自启
```

### 6. 访问系统

打开浏览器访问：
- 开发模式：`http://localhost:5173`
- 生产模式：`http://your-server-ip:5173`

默认管理员账号：`admin` / 密码：`admin123`

---

## 三、服务端配置检查清单

在前端运行 `npm run build` **之前**，需要检查以下配置：

### 3.1 后端 Session 密钥（必配）

编辑 `backend/server.js`，找到 `secret` 配置：
```javascript
app.use(session({
  secret: 'your-super-secret-key-here',  // 改为随机字符串
  // ...
}));
```

### 3.2 Vite 代理配置（可配）

编辑 `frontend/vite.config.ts`，确认代理指向正确的后端地址：
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:3001',  // 如果后端在其他服务器，改这里
    changeOrigin: true,
  }
}
```

### 3.3 企业微信 Webhook（可配）

登录系统后 → 通知设置 → 配置 Webhook URL。数据已保存在数据库的 `notification_config` 表中。

### 3.4 跨域配置

如果前端和后端部署在不同端口/域名，检查 `backend/server.js` 的 CORS 配置：
```javascript
app.use(cors({
  origin: 'http://localhost:5173',  // 改为前端实际地址
  credentials: true,
}));
```

---

## 四、数据迁移

### 数据库文件位置
```
backend/db/equipment.db   ← 主数据库（所有数据）
```

### 数据库表清单（19 张表）
| 表名 | 说明 |
|------|------|
| users | 用户账号（三级权限） |
| devices | 设备台账（179 台） |
| repairs | 维修工单（57 条） |
| parts | 零部件库存（99 种） |
| tools | 工具台账（84 件） |
| maintenance_plans | 保养计划 |
| improvement_requests | 现场改善申请 |
| ... | 其他辅助表 |

### 上传文件
```
backend/uploads/   ← 设备台账导入/导出的 Excel 文件
```
迁移时建议一并复制。

---

## 五、常见问题

**Q: 启动后访问页面空白？**
A: 先检查后端是否正常启动：访问 `http://localhost:3001/api/devices` 看是否返回 JSON 数据。

**Q: 登录后显示"未登录"或一直转圈？**
A: Session 配置需要 `sameSite: 'lax'` 或 `'strict'`，且前端请求必须带 `credentials: 'include'`。

**Q: 跨域错误？**
A: 检查 CORS 配置，确保 `origin` 与前端访问地址一致。

**Q: 数据库只读错误？**
A: 检查 `backend/db/equipment.db` 文件权限，确保运行用户有写入权限。

**Q: 端口被占用？**
A: 修改 `backend/server.js` 的端口号（默认 3001），同步修改 `frontend/vite.config.ts` 的代理目标。

---

*生成时间：2026-05-21*
*如有问题请联系系统管理员*
